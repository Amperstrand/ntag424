when:
  - event: push
  - event: pull_request
  - event: manual

steps:
  fmt:
    image: "rust:1.95"
    environment:
      CARGO_TERM_COLOR: "always"
    commands:
      - rustup component add rustfmt
      - cargo fmt --all -- --check
